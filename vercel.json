{
  "version": 2,
  "buildCommand": "cd aimakerspace && python -m pip wheel . -w dist && cd ../frontend && npm install && npm run build && cd .. && mkdir -p api/wheels && cp aimakerspace/dist/aimakerspace-0.1.0-py3-none-any.whl api/wheels/",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/**": {
      "excludeFiles": "{etl/**,tests/**,frontend/**,.venv/**,node_modules/**,.git/**,.vercel/**}",
      "includeFiles": "api/wheels/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/frontend/dist/$1"
    }
  ]
}